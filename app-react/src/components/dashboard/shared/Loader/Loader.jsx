import React from 'react';
import { Loader } from 'semantic-ui-react';

const LoaderInlineCentered = () => <Loader content="Loading..." style={{marginTop: '20%'}} active inline='centered' />

export default LoaderInlineCentered;
